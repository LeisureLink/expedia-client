machine:
  node:
    version: 6
dependencies:
  pre:
    - rm -rf ~/$CIRCLE_PROJECT_REPONAME/node_modules
deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    owner: LeisureLink
    commands:
      - npm publish
